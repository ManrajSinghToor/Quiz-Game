import fetch from "node-fetch";

// Testing public/free transactional HTTP email endpoint
async function testHttpEmail() {
  console.log("Testing HTTP API email dispatch...");
  // HTTP REST APIs work on Port 443 (HTTPS), which is NEVER blocked by Render!
}

testHttpEmail();
